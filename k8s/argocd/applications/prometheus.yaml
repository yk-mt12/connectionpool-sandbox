apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: connectionpool-prometheus
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  sources:
    # Helm chart from prometheus-community
    - repoURL: https://prometheus-community.github.io/helm-charts
      chart: prometheus
      targetRevision: "*"
      helm:
        releaseName: prometheus
        valueFiles:
          - $values/k8s/helm-values/prometheus.yaml
    # Local values file reference
    - repoURL: https://github.com/yk-mt12/connectionpool-sandbox.git
      targetRevision: HEAD
      ref: values
  destination:
    server: https://kubernetes.default.svc
    namespace: connectionpool-sandbox
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
